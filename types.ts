export interface ServiceItem {
    id: string;
    image: string;
    title: string;
    description?: string;
}

export interface ProjectItem {
    id: string;
    image: string;
    title: string;
    description: string;
}

export interface StatItem {
    value: string;
    label: string;
}
